[package]
name = "notmux-theme"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
notmux-core = { path = "../notmux-core" }
notagent_theme = { path = "../notagent-theme" }

gpui = { git = "https://github.com/zed-industries/zed", package = "gpui" }
alacritty_terminal = "0.25"

[dev-dependencies]
pretty_assertions = "1"
