import 'dart:ui';

class TerminalTheme {
  static const fontFamily = 'JetBrainsMono';
  static const defaultFontSize = 13.0;
  static const lineHeightFactor = 1.2;

  static const bgColor = Color(0xFF1E1E1E);
  static const fgColor = Color(0xFFCCCCCC);
  static const cursorColor = Color(0xFFAEAFAD);
  static const selectionColor = Color(0x40264F78);
}
